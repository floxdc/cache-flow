# 00.03-update-packages: Update NuGet packages for net9.0/net10.0 compatibility

## Objective
Update package references across all projects to versions supporting the new TFMs. Fix security vulnerability and deprecated package in CacheFlowTests.

## Done When
All packages updated; no security vulnerabilities; no deprecated packages; restore succeeds.
