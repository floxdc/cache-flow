# 00.01-sdk-prerequisites: Validate .NET SDK prerequisites

## Objective
Verify that the .NET 9 and .NET 10 SDKs are installed and that global.json (if present) is compatible.

## Done When
.NET 9 and .NET 10 SDKs confirmed installed; global.json (if present) allows both versions.
